# Scheduling doc-assistant

This project runs anywhere Python runs; it does not require LangGraph
Platform. `langgraph dev` is for local development only.

For interactive use, just run `python3 run.py "your input"`.
