schemaVersion: 1
recipe:
  id: repo-scout
  capabilities:
    required: [code.search]
    optional: []
  resourceIds: [extension.worker-submit, policy.evidence, role.repo-scout, system.worker-protocol]
  permissionProfile: observe
  workspaceMode: shared-readonly
  modelTier: fast
  tools: [read, grep, find, ls, submit_worker_result]
  budget:
    timeoutSeconds: 600
